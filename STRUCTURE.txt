BLOCKHUB_WEB/
│
├── app/
│   ├── __init__.py          # Initializes Flask, DB, and registers Blueprints
│   ├── models.py            # ALL your database classes (User, Announcement, etc.)
│   ├── forms.py             # Replaces webforms.py
│   ├── utils.py             # Replaces utility.py
│   ├── filters.py           # Replaces filter.py
│   │
│   ├── main/                # BLUEPRINT: The standard UI
│   │   ├── __init__.py      
│   │   └── routes.py        # Dashboard, Courses, Announcements views
│   │
│   ├── auth/                # BLUEPRINT: Authentication
│   │   ├── __init__.py      
│   │   └── routes.py        # Login, Signup, Logout logic
│   │
│   ├── api/                 # BLUEPRINT: JSON & Fetch routes
│   │   ├── __init__.py
│   │   └── routes.py        # /api/delete_announcement, /api/validate_url
│   │
│   ├── static/              # CSS, JS, Images (Keep as is)
│   │   ├── css
│   │   ├── img
│   │   └── js
│   │       ├──    config.js (Regex, constants)
│   │       ├──    core.js (Sidebar, dark mode, global modals)
│   │       ├──    auth.js (Login/Signup forms)
│   │       ├──    profile.js (Account settings, updating tags)
│   │       ├──    announcements.js (Pinning, heart reacts, reading)
│   │       ├──    feedbacks.js (Resolving, deleting)
│   │       ├──    main.js (The traffic cop that imports and runs everything)
│   │       └──    sw.js (Offline/Push notifications)
│   └── templates/           # HTML files (Keep as is, maybe subfolder by feature)
│
├── instance/
│   └── blockhub.db          # Your single source of truth for the DB
│
├── .env                     # Secrets (Do not upload to GitHub!)
├── .gitignore               # Ignores venv, .env, instance/blockhub.db, __pycache__
├── requirements.txt         # Your dependencies
└── run.py                   # Simply imports app and runs it